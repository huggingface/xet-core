Process for file reconstruction of file term index 1. 



- Get term metadata --> (term hash, size, etc., retrieval key, url+source info)
- Check if exists (maybe).
- Check if in cache (maybe).
- Check if 